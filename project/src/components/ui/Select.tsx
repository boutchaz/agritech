// Simple native HTML select component for backwards compatibility
// This is used by most components that just need a basic dropdown
// For advanced Radix UI Select, import from './select' (lowercase)
export { NativeSelect as Select, type NativeSelectProps as SelectProps, NativeSelect as default } from './NativeSelect'
