# Copy to `.env.integration` (gitignored) for local integration runs.
# Loaded automatically by `playwright.integration.config.ts`.

INTEGRATION_USER_EMAIL=
INTEGRATION_USER_PASSWORD=
INTEGRATION_CALIBRATION_PARCEL_ID=

# Optional
# INTEGRATION_BASE_URL=https://agritech-dashboard.thebzlab.online
# Opt-in: actually POST initial calibration from the wizard (real job on the parcel)
# INTEGRATION_CALIBRATION_SUBMIT_INITIAL=true
# Legacy alias: INTEGRATION_CALIBRATION_SUBMIT_F1=true
