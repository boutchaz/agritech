version: "3.8"
services:
  docs:
    image: nginx:alpine
    container_name: agritech-docs
    ports:
      - "8080:80"
    volumes:
      - ../build:/usr/share/nginx/html:ro
    restart: unless-stopped

