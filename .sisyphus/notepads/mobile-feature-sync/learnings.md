2026-04-02: Added a dev-only Expo Router crash-test screen at `mobile/app/(drawer)/_dev-crash-test.tsx` using `PageHeader` and shared theme tokens. The component exits early with `if (!__DEV__) return null;` so it stays out of production.
2026-04-02: Completed i18n sweep on `mobile/app/(drawer)/(accounting)/invoices/new.tsx`, `mobile/app/(drawer)/(inventory)/entries/new.tsx`, and `mobile/app/(drawer)/(pest-alerts)/new.tsx` by replacing hardcoded UI strings with `t()` keys under `common.form`, `common.empty`, `common.severity`, `common.detectionMethods`, and `common.actions`.
2026-04-02: Added matching locale keys to `mobile/src/locales/{en,fr,ar}/common.json` and verified form screen tests using `react-test-renderer` with explicit mocks for `expo-router`, `@expo/vector-icons`, hooks, and i18n.
