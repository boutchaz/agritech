/** Default number of calibration runs returned on wire (GET history, review snapshot, export). */
export const CALIBRATION_HISTORY_DEFAULT_LIMIT = 25;

/** Hard cap for `?limit=` on GET …/calibration/history. */
export const CALIBRATION_HISTORY_MAX_LIMIT = 100;
