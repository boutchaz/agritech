services:
  agritech-api:
    # Requires env: SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, JWT_SECRET,
    # DATABASE_URL, CORS_ORIGIN, SUPABASE_NETWORK_NAME, EXTERNAL_NETWORK_NAME, API_PUBLIC_HOST, etc.
    build:
      context: ..
      dockerfile: agritech-api/Dockerfile
      target: production
    container_name: agritech-api
    expose:
      - "3001"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=3001
      - API_PREFIX=${API_PREFIX:-api/v1}
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRES_IN=${JWT_EXPIRES_IN:-1d}
      - DATABASE_URL=${DATABASE_URL}
      - CORS_ORIGIN=${CORS_ORIGIN}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - RATE_LIMIT_TTL=${RATE_LIMIT_TTL:-60}
      - RATE_LIMIT_MAX=${RATE_LIMIT_MAX:-100}
      - STRAPI_API_URL=${STRAPI_API_URL}
      - STRAPI_API_TOKEN=${STRAPI_API_TOKEN}
      - ZAI_API_KEY=${ZAI_API_KEY}
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_SECURE=${SMTP_SECURE}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - EMAIL_FROM=${EMAIL_FROM}
      - FRONTEND_URL=${FRONTEND_URL}
      - SATELLITE_SERVICE_URL=${SATELLITE_SERVICE_URL}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3001/api/v1/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
    labels:
      - "traefik.enable=true"
      # Note: Router rule removed to avoid conflict with Dokploy auto-generated routers
      # Dokploy creates: agritech-nestjs-041g0r-6-web and agritech-nestjs-041g0r-6-websecure
      - "traefik.http.services.agritech-api.loadbalancer.server.port=3001"
      # Ensure Traefik passes through all headers from the application
      - "traefik.http.middlewares.agritech-api-headers.headers.customrequestheaders.X-Forwarded-Host=${API_PUBLIC_HOST}"
      # WebSocket support - required for Socket.IO connections
      - "traefik.http.middlewares.agritech-api-websocket.headers.customrequestheaders.Connection=upgrade"
      - "traefik.http.middlewares.agritech-api-websocket.headers.customrequestheaders.Upgrade=websocket"
    networks:
      - agritech-network
      - supabase-network

networks:
  agritech-network:
    external: true
    name: ${EXTERNAL_NETWORK_NAME:-dokploy-network}
  supabase-network:
    external: true
    name: ${SUPABASE_NETWORK_NAME}
